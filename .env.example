# Each visitor pastes their own Dune key (localStorage). This only carries the
# non-secret saved query ID baked in at build time.
# Saved query: "Markout — bot pool vs control" https://dune.com/queries/7733210
VITE_DUNE_QUERY_ID=7733210
